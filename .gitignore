# Dependencies
node_modules/

# Release build workspace (separate packaging directory, caches included)
DSHManagerRelease/

# electron-builder output (CI / manual builds)
release/

# Logs and runtime artifacts
*.log
crash.log
npm-debug.log*

# Git credentials (never commit)
.git-credentials
.git-credentials-*

# OS / editor files
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
